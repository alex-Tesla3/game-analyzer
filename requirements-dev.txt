# Development & test dependencies — NOT included in Docker image
# Install with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.4.0,<10
pytest-asyncio>=1.0,<2
pytest-cov>=5.0,<6
pytest-playwright>=0.5.2,<1
playwright>=1.49.0,<2
