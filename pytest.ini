[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts = -q -m "not browser"
markers =
    browser: Playwright browser E2E (requires chromium; see scripts/run_browser_e2e.sh)
filterwarnings =
    ignore::DeprecationWarning:passlib.*
